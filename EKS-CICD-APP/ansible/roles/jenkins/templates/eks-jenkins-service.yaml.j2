apiVersion: v1
kind: Service
metadata:
  name: jenkins-service
  namespace: jenkins
spec:
  type: LoadBalancer
  selector:
    app.kubernetes.io/component: jenkins-controller
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
